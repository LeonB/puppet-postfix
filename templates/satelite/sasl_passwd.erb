# MANAGED BY PUPPET!
# modules/postfix/templates/satelite/sasl_passwd.erb
# Changes WILL BE overwritten! Any permanent changes must be checked
# into git.

<%= @relayhost_password != :undefined ? "#{@relayhost} #{@relayhost_username}:#{@relayhost_password}" : '' %>
